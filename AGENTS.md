# Repository Instructions

## Userscript Versioning

- When changing `talkguest-downloader.user.js`, always bump the Tampermonkey `@version`.
- Use a minor version bump by default, for example `0.3` to `0.4`.
- Use a major version bump only when the user explicitly says the change is a major update.
